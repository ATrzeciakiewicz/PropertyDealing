var PropertyDealing = {

    //isRegister: new Boolean(),

    Register: function (s, e) {
        //var key = true;
        var url = '/Login/Register';

        window.location.href = url;// + key;
    }
};